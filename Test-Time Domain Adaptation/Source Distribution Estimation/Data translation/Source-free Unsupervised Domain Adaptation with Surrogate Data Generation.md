提出一种两阶段的无源无监督域适应方法
首先，固定源模型，通过最小化交叉熵训练损失生成代理源数据，同时强制执行基于模型的梯度约束以提升全局拟合
然后，利用生成的代理源数据，部署现有的 UDA 方法并合并未标记的目标数据，以学习能够很好泛化的目标预测模型

**生成代理源数据**
给定具有固定参数 $\Theta$ 的预测模型，可以反转训练过程来生成**可以重现给定预测模型 $F_s \circ \Phi_s(\cdot;\Theta)$ 的源训练数据集**。用 $X_t$ 初始化 $\hat{X}_s$，使用当前源预测模型下的预测标签作为相应的标签 $\hat{Y}_s$


